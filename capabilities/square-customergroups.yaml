naftiko: 1.0.0-alpha2
info:
  label: Square — Customer Groups
  description: 'Customer Groups. 5 operations. Lead operation: Square List Customer Groups. Self-contained Naftiko capability covering one Square business surface.'
  tags:
  - Square
  - Customer Groups
  created: '2026-06-03'
  modified: '2026-06-03'
binds:
- namespace: env
  keys:
    SQUARE_ACCESS_TOKEN: SQUARE_ACCESS_TOKEN
capability:
  consumes:
  - type: http
    namespace: square-customergroups
    baseUri: https://connect.squareup.com
    description: Square — Customer Groups business capability. Self-contained, no shared references.
    authentication:
      type: bearer
      token: '{{env.SQUARE_ACCESS_TOKEN}}'
    resources:
    - name: v2-customers-groups
      path: /v2/customers/groups
      operations:
      - name: ListCustomerGroups
        method: GET
        description: Square List Customer Groups
        inputParameters:
        - name: cursor
          in: query
          type: string
          required: false
          description: A pagination cursor returned by a previous call to this endpoint.
        - name: limit
          in: query
          type: integer
          required: false
          description: The maximum number of results to return in a single page. This limit is advisory. The response might contain more or fewer results.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: CreateCustomerGroup
        method: POST
        description: Square Create Customer Group
        inputParameters:
        - name: body
          in: body
          type: object
          required: true
          description: Request body payload.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: v2-customers-groups-group-id
      path: /v2/customers/groups/{group_id}
      operations:
      - name: DeleteCustomerGroup
        method: DELETE
        description: Square Delete Customer Group
        inputParameters:
        - name: group_id
          in: path
          type: string
          required: true
          description: The ID of the customer group to delete.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: RetrieveCustomerGroup
        method: GET
        description: Square Retrieve Customer Group
        inputParameters:
        - name: group_id
          in: path
          type: string
          required: true
          description: The ID of the customer group to retrieve.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: UpdateCustomerGroup
        method: PUT
        description: Square Update Customer Group
        inputParameters:
        - name: group_id
          in: path
          type: string
          required: true
          description: The ID of the customer group to update.
        - name: body
          in: body
          type: object
          required: true
          description: Request body payload.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: square-customergroups-rest
    port: 8080
    description: REST adapter for Square — Customer Groups. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/v2/customers/groups
      name: v2-customers-groups
      description: REST surface for /v2/customers/groups.
      operations:
      - method: GET
        name: ListCustomerGroups
        description: Square List Customer Groups
        call: square-customergroups.ListCustomerGroups
        with:
          cursor: rest.cursor
          limit: rest.limit
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v2/customers/groups
      name: v2-customers-groups
      description: REST surface for /v2/customers/groups.
      operations:
      - method: POST
        name: CreateCustomerGroup
        description: Square Create Customer Group
        call: square-customergroups.CreateCustomerGroup
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v2/customers/groups/{group-id}
      name: v2-customers-groups-group-id
      description: REST surface for /v2/customers/groups/{group_id}.
      operations:
      - method: DELETE
        name: DeleteCustomerGroup
        description: Square Delete Customer Group
        call: square-customergroups.DeleteCustomerGroup
        with:
          group_id: rest.group_id
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v2/customers/groups/{group-id}
      name: v2-customers-groups-group-id
      description: REST surface for /v2/customers/groups/{group_id}.
      operations:
      - method: GET
        name: RetrieveCustomerGroup
        description: Square Retrieve Customer Group
        call: square-customergroups.RetrieveCustomerGroup
        with:
          group_id: rest.group_id
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/v2/customers/groups/{group-id}
      name: v2-customers-groups-group-id
      description: REST surface for /v2/customers/groups/{group_id}.
      operations:
      - method: PUT
        name: UpdateCustomerGroup
        description: Square Update Customer Group
        call: square-customergroups.UpdateCustomerGroup
        with:
          group_id: rest.group_id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: square-customergroups-mcp
    port: 9090
    transport: http
    description: MCP adapter for Square — Customer Groups. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: list-customer-groups
      description: Square List Customer Groups
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: square-customergroups.ListCustomerGroups
      with:
        cursor: tools.cursor
        limit: tools.limit
      outputParameters:
      - type: object
        mapping: $.
    - name: create-customer-group
      description: Square Create Customer Group
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: square-customergroups.CreateCustomerGroup
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-customer-group
      description: Square Delete Customer Group
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: square-customergroups.DeleteCustomerGroup
      with:
        group_id: tools.group_id
      outputParameters:
      - type: object
        mapping: $.
    - name: retrieve-customer-group
      description: Square Retrieve Customer Group
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: square-customergroups.RetrieveCustomerGroup
      with:
        group_id: tools.group_id
      outputParameters:
      - type: object
        mapping: $.
    - name: update-customer-group
      description: Square Update Customer Group
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: square-customergroups.UpdateCustomerGroup
      with:
        group_id: tools.group_id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
